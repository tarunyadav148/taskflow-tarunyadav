# Database
DB_USERNAME=postgres
DB_PASSWORD=password123

# JWT — generate with: openssl rand -base64 32
JWT_SECRET=your_base64_encoded_secret_at_least_32_chars_long
JWT_EXPIRATION_MS=86400000

# Server
PORT=8080
